// src/utils/insights/personaInsights.ts
// Placeholder persona insight helpers (kept minimal; no refactor of current engine).

export function getPersonaLabel(persona?: string | null) {
  return persona || "The Builder";
}


